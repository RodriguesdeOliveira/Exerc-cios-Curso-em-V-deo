## Biblioteca PYTHON, MODULOS  ==  IMPORT
## import math ( ceil, floor, trunc, pow, sqrt, factorial) - inclui TODAS
## from math import sqrt,ceil - inclui UMA/DUAS
## import bebida - inclui TODAS, generalizado
## from doce import pudim  - inclui UMA, especializado


import math
num = int(input('Digite um número: '))
raiz = math.sqrt(num)
print('A raiz de {} é igual a {}'.format (num, raiz))
